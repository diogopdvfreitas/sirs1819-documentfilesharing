package a47.client.shell.service;

import org.jboss.logging.Logger;

public class LogoutService {
    private static Logger logger = Logger.getLogger(LogoutService.class);

    public boolean LogoutServer(String username) {
        return false;
    }
}
